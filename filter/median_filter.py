from PIL import Image
import numpy as np
import matplotlib.pyplot as plt


def median_filter(path, regionSize):
    '''
    Values for every pixel equals to the median of all values in the region (Folien 4 Seite 36)
    :param path:
    :param regionSize:
    :return:
    '''



if __name__ == "__main__":
