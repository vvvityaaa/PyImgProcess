from PIL import Image
import numpy as np
import matplotlib.pyplot as plt


def median_filter(path, regionSize):
    '''
    Values for every pixel equals to the median of all values in the region
    :param path:
    :param regionSize:
    :return:
    '''



if __name__ == "__main__":
